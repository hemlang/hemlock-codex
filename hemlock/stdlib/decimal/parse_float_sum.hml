import { parse_float, to_fixed } from "@stdlib/decimal";

let raw = ["1.5", "2.25", "-0.75", "3.14159", "10"];
let sum = 0.0;
for (s in raw) {
    let v = parse_float(s);
    sum += v;
    print(`"${s}" -> ${v}`);
}
print(`sum: ${to_fixed(sum, 4)}`);
